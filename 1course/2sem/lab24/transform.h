#ifndef _TRANSFORM_H_
#define _TRANSFORM_H_

#include <stdio.h>
#include <stdlib.h>

#include "struct.h"
#include "tree.h"


tree *num_num(tree *root);
tree *sign_num(tree *root);
tree *num_sigh(tree *root);
tree *sigh_sigh(tree *root);
void *divided(tree *root);


#endif
